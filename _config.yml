remote_theme: "mmistakes/minimal-mistakes@4.14.2"
plugins:
  - jekyll-include-cache
minimal_mistakes_skin: "dark"
include: ["_pages"]
defaults:
  # _pages
  - scope:
      path: ""
      type: pages
    values:
      layout: single
      author_profile: true
